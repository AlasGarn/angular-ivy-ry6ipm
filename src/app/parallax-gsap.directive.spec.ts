import { ParallaxGsapDirective } from './parallax-gsap.directive';

describe('ParallaxGsapDirective', () => {
  it('should create an instance', () => {
    const directive = new ParallaxGsapDirective();
    expect(directive).toBeTruthy();
  });
});
